# Failure Modes

## Missing metric values
Fallback: mark status as incomplete and list missing metrics.

## Historical baseline unavailable
Fallback: compare only against static thresholds.

## Metric spike detected
Fallback: escalate to operations alert agent.

## False positive risk
Fallback: require confirmation across multiple data points before alerting.
