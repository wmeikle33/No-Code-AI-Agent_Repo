# Failure Modes

## Calendar access unavailable
Fallback: ask user to provide meeting list manually.

## Meeting context missing
Fallback: summarize known calendar details and flag missing materials.

## Conflicting meeting times
Fallback: highlight conflict and suggest rescheduling.

## Sensitive meeting content
Fallback: summarize only metadata unless the user has permission.
